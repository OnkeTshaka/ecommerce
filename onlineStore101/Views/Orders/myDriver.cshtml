@model onlineStore101.Models.Order

@{
    ViewBag.Title = "myDriver";
}

<h2>My Driver Info</h2>

<div>
    <table class="table-bordered ">
        <tr style="padding:5px;text-align:center;">
            <th>Name</th>
            <th>Email</th>
            <th>Phone</th>
        </tr>

        @foreach (var item in Model.DeliveryTime)
        {
            <tr>
                @*<td>
                    <img src="data:image/png;base64,@Convert.ToBase64String(item.Product.Picture, 0, item.Product.Picture.Length)" class="img-responsive img-thumbnail" style="width: 50px; height: 50px;" alt="" />
                </td>*@
                <td>@item.Driver.Name</td>
                <td>@item.Driver.EmailAddress</td>
                <td>@item.Driver.PhoneNumber</td>
            </tr>
        }
    </table>
</div>
<p>
    @Html.ActionLink("Details2", "Details2", new { id = Model.OrderID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
